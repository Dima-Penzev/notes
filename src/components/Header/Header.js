import AddButton from "../AddButton/AddButton";
import "./Header.css";

function Header() {
  return (
    <header className="header">
      <AddButton />
    </header>
  );
}

export default Header;
